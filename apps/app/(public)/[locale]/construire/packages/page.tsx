import {
  PlaceholderPage,
  placeholderMetadata,
} from "@/components/public/content/placeholder-page";

export const metadata = placeholderMetadata("Packages");

export default function Page() {
  return <PlaceholderPage title="Packages" />;
}