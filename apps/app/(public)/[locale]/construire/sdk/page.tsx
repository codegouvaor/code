import {
  PlaceholderPage,
  placeholderMetadata,
} from "@/components/public/content/placeholder-page";

export const metadata = placeholderMetadata("SDK");

export default function Page() {
  return <PlaceholderPage title="SDK" />;
}