import {
  PlaceholderPage,
  placeholderMetadata,
} from "@/components/public/content/placeholder-page";

export const metadata = placeholderMetadata("CLI");

export default function Page() {
  return <PlaceholderPage title="CLI" />;
}