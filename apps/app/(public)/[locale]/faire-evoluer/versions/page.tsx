import {
  PlaceholderPage,
  placeholderMetadata,
} from "@/components/public/content/placeholder-page";

export const metadata = placeholderMetadata("Versions");

export default function Page() {
  return <PlaceholderPage title="Versions" />;
}